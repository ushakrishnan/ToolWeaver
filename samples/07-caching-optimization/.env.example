# Azure OpenAI Configuration (for LLM calls in examples)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_API_VERSION=2024-08-01-preview
PLANNER_PROVIDER=azure-openai
PLANNER_MODEL=gpt-4o

# Redis Configuration (Optional)
# Set USE_REDIS=true to enable real Redis, or false to use mock implementation
USE_REDIS=false
REDIS_URL=redis://redis-xxxxx.c56.east-us.azure.cloud.redislabs.com:10485
REDIS_PASSWORD=your-redis-password-here

# When USE_REDIS=false: Uses mock cache with file-based fallback (no Redis required)
# When USE_REDIS=true: Connects to real Redis with automatic file fallback on failures
